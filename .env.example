# Copie ce fichier en ".env" (ignore par git).
# En local (avec le proxy Vite configure dans vite.config.js), laisse tel quel.
# En production, comme front et back sont sur le meme domaine OVH, "/api" reste
# la bonne valeur si le backend est deploye dans un sous-dossier "api".
VITE_API_URL=/api
